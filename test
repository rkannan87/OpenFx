private void processFormattedLinesArray(String[] arrayLines, StringBuilder strBuf) {
    if (arrayLines != null) {
        for (Object line : arrayLines) {
            strBuf.append(org.apache.commons.text.StringEscapeUtils.escapeHtml4(line.toString()) + '\n');
        }
    }
}
