False Positive: The flagged value at desj.xml:24 (http://www.w3.org/2001/XMLSchema-instance) is a standard XML namespace declaration (xmlns:xsi), not a network endpoint. Namespace URIs are identifiers only and are never fetched or transmitted, so no sensitive data is sent in cleartext. Changing it to HTTPS would create a different namespace and break xsi:nil parsing. No code change required.

False Positive: The flagged value at cibc.xml:2 (http://www.w3.org/2001/XMLSchema-instance) is a standard XML namespace declaration (xmlns:xsi) on the root element, not a network endpoint. Namespace URIs are identifiers only and are never fetched or transmitted, so no sensitive data is sent in cleartext. Changing it to HTTPS would create a different namespace and break xsi:nil parsing. No code change required.
